<h2>Add a Book for Trade</h2>

<form class="add-book-form">
  <div>
    <label for="title_input">Title</label>
    <input type="text" id="title_input" maxlength="255" required/>
  </div>
  <div>
    <label for="author_first_name_input">Author's First Name</label>
    <input type="text" id="author_first_name_input" maxlength="255" required/>
  </div>
  <div>
    <label for="author_last_name_input">Author's Last Name</label>
    <input type="text" id="author_last_name_input" maxlength="255" required />
  </div>
  <div>
    <label for="genre_selection">Genre</label>
    <select id="genre_selection" required>
      <option value="" disabled selected>Choose a genre</option>
      {{#each genres as |genre|}}
        <option value="{{genre.id}}">{{genre.name}}</option>
      {{/each}}
    </select>
  </div>
  <div>
    <label for="format_selection">Format</label>
    <select id="format_selection" required>
      <option value="" disabled selected>Choose one</option>
        <option value="1">Paperback</option>
        <option value="0">Hardcover</option>
    </select>
  </div>
  <div>
    <label for="condition_selection">Condition</label>
    <select id="condition_selection" required>
      <option value="" disabled selected>Choose one</option>
        <option value="Like New">Like New</option>
        <option value="Good">Good</option>
        <option value="Well-loved">Well-loved</option>
    </select>
  </div>
  <div>
    <label for="description_input">Description</label>
    <textarea id="description_input" placeholder="Tell us a little bit about the book."></textarea>
  </div>
  <div>
    <button type="submit">publish</button>
  </div>
</form>

<script src="/js/add-book.js"></script>